
const { arrayCars, arrayNumbers, cities, info } = require("./data")
const { firstElement, withoutLastElm, leaveLast, leaveFirst, positionN, removeSpecific, uniq, sum, getRandomArbitrary, firstLetter, compareLastLetter, calculate, populationCity } = require("./functions")

// console.log(firstLetter("sean"));

console.log(populationCity());
